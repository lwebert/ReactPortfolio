//use an array of objects for project data, loop over it and create a project component for each project in the array
//use the generic project component to do this

// function Portfolio(props) {
// 	return (
// 		<div>
// 			{props.projectList.map((project) => (
// 				<Project
// 					key={project.id}
// 					projectName={project.name}
// 					projectGit={project.github}
// 					projectImg={project.image}
// 				/>
// 			))}
// 		</div>
// 	);
// }

// export default Portfolio;

// <Project id={} .../>
// in project component - then use the props we would pass here and plug them in to the HTML


//TODO: WHEN I am presented with the Portfolio section, THEN I see titled images of six of the developer’s applications with links to both the deployed applications and the corresponding GitHub repository
